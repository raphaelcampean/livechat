<div class="mb-2 p-2 rounded bg-white border">
	<strong><%= message.user.name %></strong><br>
	<span><%= message.body %></span>
	<small class="text-muted float-end"><%= message.created_at.strftime("%H:%M") %></small>
</div>